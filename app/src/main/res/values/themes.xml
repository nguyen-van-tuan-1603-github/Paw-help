<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.PawHelp" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="colorPrimary">@color/button_primary</item>

        <item name="colorPrimaryContainer">@color/button_end</item>

        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/button_start</item>

    </style>

    <style name="Theme.PawHelp" parent="Base.Theme.PawHelp" />

    <!-- Theme for Splash Screen -->
    <style name="Theme.PawHelp.NoActionBar" parent="Theme.PawHelp">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>